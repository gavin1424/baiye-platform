import { createSignedContractPdf } from "./contract-pdf.js";

const E=new TextEncoder(),D=new TextDecoder();
const json=(data,status=200,headers={})=>new Response(JSON.stringify(data),{status,headers:{"content-type":"application/json; charset=UTF-8",...headers}});
const id=(prefix)=>`${prefix}_${crypto.randomUUID()}`;
const now=()=>new Date().toISOString();
const b64=(value)=>btoa(String.fromCharCode(...new Uint8Array(value))).replaceAll("+","-").replaceAll("/","_").replaceAll("=","");
const ub64=(value)=>Uint8Array.from(atob(value.replaceAll("-","+").replaceAll("_","/")+"===".slice((value.length+3)%4)),character=>character.charCodeAt(0));
async function hmac(value,secret){const key=await crypto.subtle.importKey("raw",E.encode(secret),{name:"HMAC",hash:"SHA-256"},false,["sign"]);return b64(await crypto.subtle.sign("HMAC",key,E.encode(value)));}
async function hash(value){return b64(await crypto.subtle.digest("SHA-256",E.encode(value)));}
async function passwordHash(password,salt,pepper){const key=await crypto.subtle.importKey("raw",E.encode(`partner-password-v1:${salt}:${pepper}`),{name:"HMAC",hash:"SHA-256"},false,["sign"]);return b64(await crypto.subtle.sign("HMAC",key,E.encode(password)));}
async function body(request){try{return await request.json();}catch{return {};}}
const clientIp=(request)=>request.headers.get("CF-Connecting-IP")||null;
function validSignature(value){try{const signature=JSON.parse(value);return Array.isArray(signature.strokes)&&signature.strokes.some(stroke=>Array.isArray(stroke)&&stroke.length>=2)&&String(value).length<100000;}catch{return false;}}
async function audit(db,request,actorType,actorId,action,entityType,entityId,metadata={}){await db.prepare("INSERT INTO audit_logs (id,actor_type,actor_id,action,entity_type,entity_id,metadata,ip_address) VALUES (?,?,?,?,?,?,?,?)").bind(id("audit"),actorType,actorId,action,entityType,entityId,JSON.stringify(metadata),clientIp(request)).run();}
async function activeContract(db){return db.prepare("SELECT * FROM contract_versions WHERE is_active=1 ORDER BY effective_date DESC LIMIT 1").first();}
async function partnerSession(partner,env){const payload=b64(E.encode(JSON.stringify({partner_id:partner.id,exp:Date.now()+7*864e5})));return `${payload}.${await hmac(payload,env.PARTNER_SESSION_SECRET)}`;}
async function partnerAuth(request,env){const token=(request.headers.get("cookie")||"").match(/(?:^|;\s*)partner_session=([^;]+)/)?.[1];if(!token||!env.PARTNER_SESSION_SECRET)return null;const[payload,signature]=token.split(".");if(!payload||signature!==await hmac(payload,env.PARTNER_SESSION_SECRET))return null;try{const decoded=JSON.parse(D.decode(ub64(payload)));return decoded.exp>Date.now()?decoded.partner_id:null;}catch{return null;}}
async function financeAdmin(request,env){const token=request.headers.get("authorization")?.replace(/^Bearer\s+/i,"");if(!token||!env.FINANCE_SESSION_SECRET)return false;const[payload,signature]=token.split(".");if(!payload||signature!==await hmac(payload,env.FINANCE_SESSION_SECRET))return false;try{return JSON.parse(D.decode(ub64(payload))).exp>Date.now();}catch{return false;}}

export function commissionTier(number){return number<=10?1000:number<=60?1500:number<=120?2000:3000;}
export async function awardCommissionForOrder(db,orderId,paymentId){const order=await db.prepare("SELECT * FROM orders WHERE id=?").bind(orderId).first();if(!order?.partner_id||order.payment_status!=="paid")return;const existing=await db.prepare("SELECT id FROM commissions WHERE order_id=? AND commission_type='sales'").bind(orderId).first();if(existing)return;const prior=await db.prepare("SELECT COUNT(*) count FROM commissions WHERE partner_id=? AND commission_type='sales' AND status IN ('confirmed','payable','paid')").bind(order.partner_id).first(),tier=commissionTier(Number(prior.count)+1);await db.prepare("INSERT INTO commissions (id,partner_id,order_id,payment_id,commission_type,base_amount,final_amount,tier,status,earned_at,confirmed_at) VALUES (?,?,?,?,?,?,?,?,?,?,?)").bind(id("commission"),order.partner_id,orderId,paymentId,"sales",tier,tier,Number(prior.count)+1,"confirmed",now(),now()).run();await db.prepare("UPDATE partners SET total_valid_sales=total_valid_sales+1,total_sales_amount=total_sales_amount+?,updated_at=? WHERE id=?").bind(order.amount_due,now(),order.partner_id).run();}
export async function reverseOrderCommission(db,orderId,reason="customer_refund"){const rows=await db.prepare("SELECT * FROM commissions WHERE order_id=? AND status NOT IN ('reversed','cancelled')").bind(orderId).all();for(const commission of rows.results){const locked=await db.prepare("SELECT s.id FROM settlement_items si JOIN settlements s ON s.id=si.settlement_id WHERE si.commission_id=? AND s.status IN ('approved','paid') LIMIT 1").bind(commission.id).first(),amount=-Number(commission.final_amount);await db.prepare("INSERT INTO commission_adjustments (id,commission_id,reason,amount,status) VALUES (?,?,?,?,?)").bind(id("adjust"),commission.id,reason,amount,locked?"confirmed":"cancelled").run();if(locked)continue;await db.prepare("UPDATE commissions SET adjustment_amount=adjustment_amount+?,final_amount=0,status='reversed',updated_at=? WHERE id=?").bind(amount,now(),commission.id).run();if(commission.commission_type==="sales")await db.prepare("UPDATE partners SET total_valid_sales=MAX(0,total_valid_sales-1),total_sales_amount=MAX(0,total_sales_amount-?),updated_at=? WHERE id=?").bind(commission.base_amount,now(),commission.partner_id).run();}}

async function partnerDashboard(db,partnerId){const partner=await db.prepare("SELECT partner_code,display_name,legal_name,referral_code,total_valid_sales,total_sales_amount,contract_status,contract_version,contract_signed_at FROM partners WHERE id=?").bind(partnerId).first(),commissions=await db.prepare("SELECT COALESCE(SUM(CASE WHEN status='confirmed' THEN final_amount ELSE 0 END),0) pending,COALESCE(SUM(CASE WHEN status='payable' THEN final_amount ELSE 0 END),0) payable,COALESCE(SUM(CASE WHEN status='paid' THEN final_amount ELSE 0 END),0) paid,COALESCE(SUM(CASE WHEN status='reversed' THEN adjustment_amount ELSE 0 END),0) reversed FROM commissions WHERE partner_id=?").bind(partnerId).first(),contract=await activeContract(db),signature=contract?await db.prepare("SELECT id,pdf_hash,signed_at FROM contract_signatures WHERE partner_id=? AND contract_version_id=? ORDER BY signed_at DESC LIMIT 1").bind(partnerId,contract.id).first():null,count=Number(partner.total_valid_sales||0);return{partner,commissions,current_tier:commissionTier(Math.max(1,count)),next_tier:count<10?10-count:count<60?60-count:count<120?120-count:0,contract:{version:contract?.version||null,signed:Boolean(signature),signature_id:signature?.id||null,signed_at:signature?.signed_at||null}};}
async function createSettlement(db,partnerId,start,end){const duplicate=await db.prepare("SELECT id FROM settlements WHERE partner_id=? AND period_start=? AND period_end=? AND status!='cancelled'").bind(partnerId,start,end).first();if(duplicate)throw new Error("此期間已建立結算單。");const commissions=(await db.prepare("SELECT c.* FROM commissions c WHERE c.partner_id=? AND c.status IN ('confirmed','payable') AND datetime(c.earned_at)>=datetime(?) AND datetime(c.earned_at)<=datetime(?) AND NOT EXISTS (SELECT 1 FROM settlement_items si JOIN settlements s ON s.id=si.settlement_id WHERE si.commission_id=c.id AND s.status IN ('approved','paid'))").bind(partnerId,start,end).all()).results,adjustments=(await db.prepare("SELECT a.* FROM commission_adjustments a JOIN commissions c ON c.id=a.commission_id WHERE c.partner_id=? AND a.status='confirmed' AND a.settlement_id IS NULL AND datetime(a.created_at)>=datetime(?) AND datetime(a.created_at)<=datetime(?)").bind(partnerId,start,end).all()).results,gross=commissions.reduce((sum,row)=>sum+Number(row.base_amount),0),service=commissions.reduce((sum,row)=>sum+Number(row.service_bonus),0),adjustment=adjustments.reduce((sum,row)=>sum+Number(row.amount),0),refund=adjustments.filter(row=>String(row.reason).includes("refund")).reduce((sum,row)=>sum+Number(row.amount),0),settlementId=id("settlement"),statements=[db.prepare("INSERT INTO settlements (id,settlement_no,partner_id,period_start,period_end,gross_commission,service_bonus,refund_deduction,other_adjustment,net_payable,status) VALUES (?,?,?,?,?,?,?,?,?,?,?)").bind(settlementId,`SET-${Date.now()}-${crypto.randomUUID().slice(0,6).toUpperCase()}`,partnerId,start,end,gross,service,refund,adjustment-refund,commissions.reduce((sum,row)=>sum+Number(row.final_amount),0)+adjustment,"draft")];for(const row of commissions)statements.push(db.prepare("INSERT INTO settlement_items (id,settlement_id,commission_id,amount) VALUES (?,?,?,?)").bind(id("settleitem"),settlementId,row.id,row.final_amount));for(const row of adjustments){statements.push(db.prepare("INSERT INTO settlement_adjustment_items (id,settlement_id,adjustment_id,amount) VALUES (?,?,?,?)").bind(id("settleadjust"),settlementId,row.id,row.amount));statements.push(db.prepare("UPDATE commission_adjustments SET settlement_id=? WHERE id=?").bind(settlementId,row.id));}await db.batch(statements);return db.prepare("SELECT * FROM settlements WHERE id=?").bind(settlementId).first();}

export async function handlePartnerRequest(request,env,url,cors){
 const db=env.FINANCE_DB,path=url.pathname;if(!db)return json({error:"財務資料庫目前無法使用。"},503,cors);
 if(path==="/api/partner/apply"&&request.method==="POST"){const input=await body(request);if(!input.legal_name||!input.email||!input.phone||!input.consent)return json({error:"請完整填寫必填資料，並確認獨立承攬／居間合作聲明。"},400,cors);const partnerId=id("partner"),partnerCode=`AG${String(Date.now()).slice(-6)}`;try{await db.prepare("INSERT INTO partners (id,partner_code,legal_name,display_name,email,phone,company_name,tax_id,status,referral_code) VALUES (?,?,?,?,?,?,?,?,?,?)").bind(partnerId,partnerCode,String(input.legal_name).slice(0,80),String(input.display_name||input.legal_name).slice(0,80),String(input.email).toLowerCase(),String(input.phone).slice(0,30),input.company_name||null,input.tax_id||null,"pending_contract",partnerCode).run();await audit(db,request,"partner",partnerId,"partner_applied","partner",partnerId,{independent_contractor_consent:true});return json({id:partnerId,partner_code:partnerCode,status:"pending_contract"},201,cors);}catch{return json({error:"此 Email 已申請過合作夥伴。若您已收到啟用通知，請前往夥伴登入；若尚未完成審核，請等待管理員審核。",code:"PARTNER_EMAIL_EXISTS"},409,cors);}}
 if(path==="/api/partner/invite/validate"&&request.method==="POST"){const input=await body(request);if(!input.token)return json({error:"請提供啟用連結。"},400,cors);const invite=await db.prepare("SELECT i.expires_at,p.legal_name,p.display_name,p.email,p.status,p.approved_at FROM partner_invites i JOIN partners p ON p.id=i.partner_id WHERE i.token_hash=? AND i.used_at IS NULL AND i.expires_at>? LIMIT 1").bind(await hash(String(input.token)),now()).first();if(!invite||invite.status!=="pending_contract"||!invite.approved_at)return json({error:"啟用連結無效、已使用、已過期或帳號尚未核准。"},401,cors);return json({legal_name:invite.legal_name,display_name:invite.display_name,email:invite.email,expires_at:invite.expires_at},200,cors);}
 if(path==="/api/partner/accept-invite"&&request.method==="POST"){const input=await body(request);if(!input.token||String(input.password||"").length<12||!env.PARTNER_SESSION_SECRET)return json({error:"密碼至少需要 12 個字元，且啟用連結必須有效。"},400,cors);const invite=await db.prepare("SELECT i.*,p.status,p.approved_at FROM partner_invites i JOIN partners p ON p.id=i.partner_id WHERE i.token_hash=? AND i.used_at IS NULL AND i.expires_at>? LIMIT 1").bind(await hash(String(input.token)),now()).first();if(!invite||invite.status!=="pending_contract"||!invite.approved_at)return json({error:"啟用連結無效、已使用、已過期或帳號尚未核准。"},401,cors);const salt=crypto.randomUUID(),password=await passwordHash(String(input.password),salt,env.PARTNER_SESSION_SECRET);try{await db.batch([db.prepare("UPDATE partners SET password_hash=?,password_salt=?,status='active',updated_at=? WHERE id=?").bind(password,salt,now(),invite.partner_id),db.prepare("UPDATE partner_invites SET used_at=? WHERE id=?").bind(now(),invite.id)]);await audit(db,request,"partner",invite.partner_id,"partner_activated","partner",invite.partner_id,{invite_id:invite.id});return json({ok:true,status:"active"},200,cors);}catch{return json({error:"帳號啟用暫時無法完成，請稍後再試。"},503,cors);}}
 if(path==="/api/partner/login"&&request.method==="POST"){const input=await body(request),email=String(input.email||"").toLowerCase(),recent=await db.prepare("SELECT COUNT(*) count FROM partner_login_attempts WHERE email=? AND attempted_at>datetime('now','-15 minutes')").bind(email).first();if(Number(recent.count)>=5)return json({error:"登入嘗試次數過多，請 15 分鐘後再試。"},429,cors);const partner=await db.prepare("SELECT * FROM partners WHERE email=?").bind(email).first(),valid=Boolean(partner?.password_hash&&env.PARTNER_SESSION_SECRET&&await passwordHash(String(input.password||""),partner.password_salt,env.PARTNER_SESSION_SECRET)===partner.password_hash);await db.prepare("INSERT INTO partner_login_attempts (id,email,success) VALUES (?,?,?)").bind(id("login"),email,valid?1:0).run();if(!valid||partner.status!=="active")return json({error:"帳號密碼錯誤，或合作夥伴帳號尚未啟用。"},401,cors);await audit(db,request,"partner",partner.id,"partner_login","partner",partner.id);return json({ok:true},200,{...cors,"set-cookie":`partner_session=${await partnerSession(partner,env)}; HttpOnly; Secure; SameSite=None; Partitioned; Path=/api/partner; Max-Age=604800`});}
 if(path==="/api/partner/attribution"&&request.method==="POST"){const input=await body(request),referral=String(input.referral_code||""),partner=await db.prepare("SELECT id,status FROM partners WHERE referral_code=?").bind(referral).first();if(!partner||partner.status!=="active"||!input.lead_name)return json({error:"推薦連結無效，或請填寫聯絡人姓名。"},400,cors);const existing=await db.prepare("SELECT id FROM partner_leads WHERE lead_email=? AND lead_email IS NOT NULL ORDER BY first_seen_at LIMIT 1").bind(input.lead_email||null).first();if(existing)return json({lead_id:existing.id,attribution:"existing"},200,cors);const leadId=id("lead");await db.prepare("INSERT INTO partner_leads (id,partner_id,lead_name,lead_phone,lead_email,source,status) VALUES (?,?,?,?,?,?,?)").bind(leadId,partner.id,String(input.lead_name).slice(0,100),input.lead_phone||null,input.lead_email||null,"referral","new").run();await audit(db,request,"system",null,"lead_attributed","partner_lead",leadId,{partner_id:partner.id,source:"referral"});return json({lead_id:leadId,attribution:"created"},201,cors);}
 if(path==="/api/partner/logout"&&request.method==="POST")return json({ok:true},200,{...cors,"set-cookie":"partner_session=; HttpOnly; Secure; SameSite=None; Partitioned; Path=/api/partner; Max-Age=0"});
 if(path.startsWith("/api/admin/")){
  if(!(await financeAdmin(request,env)))return json({error:"需要財務管理員授權。"},401,cors);
  const adminPdf=path.match(/^\/api\/admin\/contracts\/([^/]+)\/pdf$/);if(adminPdf&&request.method==="GET"){const signature=await db.prepare("SELECT * FROM contract_signatures WHERE id=?").bind(adminPdf[1]).first();if(!signature?.pdf_object_key||!env.CONTRACTS_BUCKET)return json({error:"找不到已簽契約 PDF。"},404,cors);const object=await env.CONTRACTS_BUCKET.get(signature.pdf_object_key);if(!object)return json({error:"找不到已簽契約 PDF。"},404,cors);await audit(db,request,"admin","finance","contract_downloaded","contract_signature",signature.id);return new Response(object.body,{headers:{...cors,"content-type":"application/pdf","content-disposition":`attachment; filename=contract-${signature.id}.pdf`,"x-pdf-sha256":signature.pdf_hash}});}
  if(path==="/api/admin/partners"&&request.method==="GET"){const search=`%${url.searchParams.get("q")||""}%`,rows=await db.prepare("SELECT id,partner_code,legal_name,display_name,email,phone,company_name,tax_id,status,approved_at,contract_status,contract_version,contract_signed_at,total_valid_sales,total_sales_amount,created_at FROM partners WHERE partner_code LIKE ? OR legal_name LIKE ? OR email LIKE ? ORDER BY created_at DESC").bind(search,search,search).all();return json({items:rows.results},200,cors);}
  const match=path.match(/^\/api\/admin\/partners\/([^/]+)(?:\/(invite|commissions|contracts))?$/);if(match&&request.method==="GET"){const partner=await db.prepare("SELECT * FROM partners WHERE id=?").bind(match[1]).first();if(!partner)return json({error:"找不到合作夥伴資料。"},404,cors);if(match[2]==="commissions")return json({partner,items:(await db.prepare("SELECT * FROM commissions WHERE partner_id=? ORDER BY created_at DESC").bind(match[1]).all()).results},200,cors);if(match[2]==="contracts")return json({partner,items:(await db.prepare("SELECT s.*,v.version,v.title FROM contract_signatures s JOIN contract_versions v ON v.id=s.contract_version_id WHERE s.partner_id=? ORDER BY s.signed_at DESC").bind(match[1]).all()).results},200,cors);return json(partner,200,cors);}
  if(match&&!match[2]&&request.method==="PATCH"){const input=await body(request),partner=await db.prepare("SELECT * FROM partners WHERE id=?").bind(match[1]).first();if(!partner)return json({error:"找不到合作夥伴資料。"},404,cors);const action=input.action||input.status;if(action==="approve"){if(partner.status!=="pending_contract")return json({error:"目前狀態無法執行核准。"},409,cors);const approvedAt=now();await db.prepare("UPDATE partners SET approved_at=COALESCE(approved_at,?),approved_by=?,updated_at=? WHERE id=?").bind(approvedAt,"finance",now(),partner.id).run();await audit(db,request,"admin","finance","partner_approved","partner",partner.id);return json({ok:true,status:"pending_contract",approved_at:approvedAt},200,cors);}const transitions={reject:"rejected",suspended:"suspended",terminated:"terminated"},status=transitions[action];if(!status)return json({error:"不允許的合作夥伴狀態操作。"},400,cors);await db.prepare("UPDATE partners SET status=?,updated_at=? WHERE id=?").bind(status,now(),partner.id).run();await audit(db,request,"admin","finance",`partner_${action}`,"partner",partner.id);return json({ok:true,status},200,cors);}
  if(match&&match[2]==="invite"&&request.method==="POST"){const partner=await db.prepare("SELECT * FROM partners WHERE id=?").bind(match[1]).first();if(!partner)return json({error:"找不到合作夥伴資料。"},404,cors);if(partner.status!=="pending_contract"||!partner.approved_at)return json({error:"請先核准此申請，才能產生啟用邀請。"},409,cors);const issuedAt=now(),expiresAt=new Date(Date.now()+72*3600e3).toISOString(),raw=crypto.randomUUID()+crypto.randomUUID();await db.batch([db.prepare("UPDATE partner_invites SET used_at=? WHERE partner_id=? AND used_at IS NULL AND expires_at>? ").bind(issuedAt,partner.id,issuedAt),db.prepare("INSERT INTO partner_invites (id,partner_id,token_hash,expires_at) VALUES (?,?,?,?)").bind(id("invite"),partner.id,await hash(raw),expiresAt)]);await audit(db,request,"admin","finance","partner_invited","partner",partner.id,{expires_at:expiresAt});return json({invite_url:`https://baiyeconnect.com/#/partner/activate?token=${encodeURIComponent(raw)}`,expires_at:expiresAt},201,cors);}
  if(path==="/api/admin/settlements"&&request.method==="GET")return json({items:(await db.prepare("SELECT s.*,p.display_name,p.partner_code FROM settlements s JOIN partners p ON p.id=s.partner_id ORDER BY s.created_at DESC").all()).results},200,cors);
  if(path==="/api/admin/settlements"&&request.method==="POST"){const input=await body(request);if(!input.partner_id||!input.period_start||!input.period_end)return json({error:"請提供合作夥伴與結算期間。"},400,cors);try{const result=await createSettlement(db,String(input.partner_id),String(input.period_start),String(input.period_end));await audit(db,request,"admin","finance","settlement_created","settlement",result.id,{partner_id:input.partner_id});return json(result,201,cors);}catch(error){return json({error:error.message||"無法建立結算單。"},409,cors);}}
  const settlement=path.match(/^\/api\/admin\/settlements\/([^/]+)$/);if(settlement&&request.method==="PATCH"){const input=await body(request),current=await db.prepare("SELECT * FROM settlements WHERE id=?").bind(settlement[1]).first();if(!current)return json({error:"找不到結算單。"},404,cors);if(input.status==="approved"&&current.status==="draft"){await db.batch([db.prepare("UPDATE settlements SET status='approved',approved_at=? WHERE id=?").bind(now(),current.id),db.prepare("UPDATE commissions SET status='payable',updated_at=? WHERE id IN (SELECT commission_id FROM settlement_items WHERE settlement_id=?) AND status='confirmed'").bind(now(),current.id)]);await audit(db,request,"admin","finance","settlement_approved","settlement",current.id);return json({ok:true,status:"approved"},200,cors);}if(input.status==="paid"&&current.status==="approved"){const paidAt=now();await db.batch([db.prepare("UPDATE settlements SET status='paid',paid_at=?,paid_method=?,paid_reference=?,paid_by=? WHERE id=?").bind(paidAt,input.paid_method||null,input.paid_reference||null,"finance",current.id),db.prepare("UPDATE commissions SET status='paid',paid_at=?,updated_at=? WHERE id IN (SELECT commission_id FROM settlement_items WHERE settlement_id=?)").bind(paidAt,paidAt,current.id)]);await audit(db,request,"admin","finance","settlement_paid","settlement",current.id,{paid_method:input.paid_method||null});return json({ok:true,status:"paid"},200,cors);}if(input.status==="cancelled"&&current.status==="draft"){await db.batch([db.prepare("UPDATE commission_adjustments SET settlement_id=NULL WHERE settlement_id=?").bind(current.id),db.prepare("UPDATE settlements SET status='cancelled' WHERE id=?").bind(current.id)]);await audit(db,request,"admin","finance","settlement_cancelled","settlement",current.id);return json({ok:true,status:"cancelled"},200,cors);}return json({error:"不允許的結算單狀態轉換。"},409,cors);}
  const convert=path.match(/^\/api\/admin\/leads\/([^/]+)\/convert$/);if(convert&&request.method==="POST"){const input=await body(request),lead=await db.prepare("SELECT * FROM partner_leads WHERE id=?").bind(convert[1]).first();if(!lead)return json({error:"找不到推薦客戶資料。"},404,cors);if(!input.merchant_name||!Number.isFinite(Number(input.amount_due))||Number(input.amount_due)<0)return json({error:"請提供商家名稱與正確金額。"},400,cors);const merchantId=id("merchant"),orderId=id("order"),merchantCode=String(input.merchant_code||`M${Date.now()}`).slice(0,40),orderNo=String(input.order_no||`ORD-${Date.now()}`).slice(0,60);await db.batch([db.prepare("INSERT INTO merchants (id,merchant_code,name,contact_name,phone,email,status) VALUES (?,?,?,?,?,?,?)").bind(merchantId,merchantCode,String(input.merchant_name).slice(0,100),lead.lead_name,lead.lead_phone,lead.lead_email,"active"),db.prepare("INSERT INTO orders (id,order_no,merchant_id,title,amount_due,currency,payment_status,partner_id,lead_id) VALUES (?,?,?,?,?,?,?,?,?)").bind(orderId,orderNo,merchantId,String(input.title||"網站建置方案").slice(0,120),Number(input.amount_due),input.currency||"TWD","unpaid",lead.partner_id,lead.id),db.prepare("UPDATE partner_leads SET merchant_id=?,status='converted',converted_at=? WHERE id=?").bind(merchantId,now(),lead.id)]);await audit(db,request,"admin","finance","lead_converted","partner_lead",lead.id,{merchant_id:merchantId,order_id:orderId,partner_id:lead.partner_id});return json({merchant_id:merchantId,order_id:orderId,partner_id:lead.partner_id},201,cors);}
  const attribution=path.match(/^\/api\/admin\/leads\/([^/]+)\/attribution$/);if(attribution&&request.method==="PATCH"){const input=await body(request),lead=await db.prepare("SELECT * FROM partner_leads WHERE id=?").bind(attribution[1]).first(),partner=await db.prepare("SELECT id FROM partners WHERE id=? AND status='active'").bind(input.partner_id||"").first();if(!lead||!partner)return json({error:"找不到推薦客戶或已啟用的合作夥伴。"},404,cors);await db.batch([db.prepare("UPDATE partner_leads SET partner_id=? WHERE id=?").bind(partner.id,lead.id),db.prepare("UPDATE orders SET partner_id=? WHERE lead_id=?").bind(partner.id,lead.id),db.prepare("INSERT INTO partner_attribution_audits (id,lead_id,previous_partner_id,new_partner_id,actor_id,reason) VALUES (?,?,?,?,?,?)").bind(id("attribution"),lead.id,lead.partner_id,partner.id,"finance",String(input.reason||"管理員歸屬修正").slice(0,300))]);await audit(db,request,"admin","finance","lead_attribution_changed","partner_lead",lead.id,{from_partner_id:lead.partner_id,to_partner_id:partner.id,reason:input.reason||null});return json({ok:true},200,cors);}
  const bonus=path.match(/^\/api\/admin\/commissions\/([^/]+)\/service-bonus$/);if(bonus&&request.method==="POST"){const input=await body(request),amount=Number(input.amount);if(!Number.isFinite(amount)||amount<0||amount>1000)return json({error:"輔導獎金必須介於 0 至 1,000 元。"},400,cors);await db.prepare("UPDATE commissions SET service_bonus=?,final_amount=base_amount+?+adjustment_amount,updated_at=? WHERE id=?").bind(amount,amount,now(),bonus[1]).run();await audit(db,request,"admin","finance","commission_service_bonus_changed","commission",bonus[1],{amount});return json({ok:true},200,cors);}
  const auditMatch=path==="/api/admin/audit"?null:path.match(/^\/api\/admin\/partners\/([^/]+)\/audit$/);if((path==="/api/admin/audit"||auditMatch)&&request.method==="GET"){const partnerId=auditMatch?.[1]||url.searchParams.get("partner")||null,rows=await db.prepare(partnerId?"SELECT id,actor_type,actor_id,action,entity_type,entity_id,metadata,created_at FROM audit_logs WHERE (entity_type='partner' AND entity_id=?) OR entity_id IN (SELECT id FROM contract_signatures WHERE partner_id=?) OR entity_id IN (SELECT id FROM partner_leads WHERE partner_id=?) OR entity_id IN (SELECT id FROM orders WHERE partner_id=?) OR entity_id IN (SELECT id FROM commissions WHERE partner_id=?) OR entity_id IN (SELECT id FROM settlements WHERE partner_id=?) ORDER BY created_at DESC LIMIT 100":"SELECT id,actor_type,actor_id,action,entity_type,entity_id,metadata,created_at FROM audit_logs ORDER BY created_at DESC LIMIT 100").bind(...(partnerId?[partnerId,partnerId,partnerId,partnerId,partnerId,partnerId]:[])).all();return json({items:rows.results},200,cors);}
 }
 const partnerId=await partnerAuth(request,env);if(!partnerId)return json({error:"請先登入夥伴中心。"},401,cors);const partner=await db.prepare("SELECT * FROM partners WHERE id=?").bind(partnerId).first();if(!partner||partner.status!=="active")return json({error:"合作夥伴帳號目前尚未啟用。"},403,cors);
 if(path==="/api/partner/me")return json({id:partner.id,partner_code:partner.partner_code,legal_name:partner.legal_name,display_name:partner.display_name,status:partner.status,referral_code:partner.referral_code},200,cors);
 if(path==="/api/partner/contract/current"&&request.method==="GET")return json(await activeContract(db),200,cors);
 if(path==="/api/partner/contract/sign"&&request.method==="POST"){const input=await body(request),contract=await activeContract(db);if(!contract)return json({error:"目前沒有可簽署的合作契約版本。"},503,cors);if(!input.read||!input.electronic||!input.independent||input.legal_name!==partner.legal_name||!validSignature(input.signature))return json({error:"請完成閱讀、電子簽署、獨立承攬確認、法定姓名及手寫簽名。"},400,cors);const existing=await db.prepare("SELECT id FROM contract_signatures WHERE partner_id=? AND contract_version_id=?").bind(partnerId,contract.id).first();if(existing)return json({error:"您已簽署目前有效的合作契約。"},409,cors);if(!env.CONTRACTS_BUCKET)return json({error:"私有契約保存服務尚未啟用。"},503,cors);const signatureId=id("sign"),signedAt=now(),signatureHash=await hash(String(input.signature)),pdf=await createSignedContractPdf({contractId:signatureId,version:contract.version,contentHtml:contract.content_html,contractHash:contract.content_hash,signatureHash,legalName:partner.legal_name,signedAt,consentVersion:"v1",signature:input.signature}),key=`contracts/${partnerId}/${contract.version}/${signatureId}.pdf`;try{await env.CONTRACTS_BUCKET.put(key,pdf.bytes,{httpMetadata:{contentType:"application/pdf",contentDisposition:`attachment; filename=contract-${signatureId}.pdf`}});}catch{return json({error:"已簽契約 PDF 保存失敗，請稍後再試。"},503,cors);}await db.prepare("INSERT INTO contract_signatures (id,partner_id,contract_version_id,legal_name,signed_at,ip_address,user_agent,contract_content_hash,signature_hash,signature_data,pdf_object_key,pdf_hash,document_hash,consent_version) VALUES (?,?,?,?,?,?,?,?,?,?,?,?,?,?)").bind(signatureId,partnerId,contract.id,partner.legal_name,signedAt,clientIp(request),request.headers.get("user-agent"),contract.content_hash,signatureHash,String(input.signature),key,pdf.pdfHash,pdf.documentHash,"v1").run();await db.prepare("UPDATE partners SET contract_status='signed',contract_version=?,contract_signed_at=?,updated_at=? WHERE id=?").bind(contract.version,signedAt,now(),partnerId).run();await audit(db,request,"partner",partnerId,"contract_signed","contract_signature",signatureId,{version:contract.version,signature_hash:signatureHash,pdf_hash:pdf.pdfHash,document_hash:pdf.documentHash,legal_review_required:true});return json({ok:true,signature_id:signatureId,pdf_hash:pdf.pdfHash,document_hash:pdf.documentHash,legal_review_required:true},201,cors);}
 const pdfMatch=path.match(/^\/api\/partner\/contracts\/([^/]+)\/pdf$/);if(pdfMatch&&request.method==="GET"){const signature=await db.prepare("SELECT * FROM contract_signatures WHERE id=? AND partner_id=?").bind(pdfMatch[1],partnerId).first();if(!signature?.pdf_object_key||!env.CONTRACTS_BUCKET)return json({error:"找不到已簽契約 PDF。"},404,cors);const object=await env.CONTRACTS_BUCKET.get(signature.pdf_object_key);if(!object)return json({error:"找不到已簽契約 PDF。"},404,cors);await audit(db,request,"partner",partnerId,"contract_downloaded","contract_signature",signature.id);return new Response(object.body,{headers:{...cors,"content-type":"application/pdf","content-disposition":`attachment; filename=contract-${signature.id}.pdf`,"x-pdf-sha256":signature.pdf_hash}});}
 if(path==="/api/partner/dashboard")return json(await partnerDashboard(db,partnerId),200,cors);if(path==="/api/partner/referral")return json({referral_code:partner.referral_code,url:`https://baiyeconnect.com/join?ref=${partner.referral_code}`},200,cors);if(path==="/api/partner/leads")return json({items:(await db.prepare("SELECT id,lead_name,source,status,created_at,converted_at FROM partner_leads WHERE partner_id=? ORDER BY created_at DESC").bind(partnerId).all()).results},200,cors);if(path==="/api/partner/orders")return json({items:(await db.prepare("SELECT order_no,title,amount_due,amount_paid,payment_status,created_at FROM orders WHERE partner_id=? ORDER BY created_at DESC").bind(partnerId).all()).results},200,cors);if(path==="/api/partner/commissions")return json({items:(await db.prepare("SELECT c.*,o.order_no,o.title FROM commissions c JOIN orders o ON o.id=c.order_id WHERE c.partner_id=? ORDER BY c.created_at DESC").bind(partnerId).all()).results},200,cors);if(path==="/api/partner/settlements")return json({items:(await db.prepare("SELECT * FROM settlements WHERE partner_id=? ORDER BY created_at DESC").bind(partnerId).all()).results},200,cors);return json({error:"找不到此服務。"},404,cors);
}
